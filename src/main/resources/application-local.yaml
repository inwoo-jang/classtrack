# ============================================================
# 개발 환경. 관찰하기 쉬운 쪽으로 설정한다.
# ============================================================

spring:
  jpa:
    hibernate:
      # 엔티티를 고치면 테이블도 따라 바뀐다. 개발 중에만 안전한 설정
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  web:
    error:
      # 검증 메시지를 화면에서 보여주기 위해 켠다
      include-message: always
      include-binding-errors: always
      include-stacktrace: never

management:
  endpoint:
    health:
      show-details: always

# 개발 중에는 항상 켠다
app:
  dev:
    enabled: true

logging:
  level:
    root: INFO
    com.inwoo.classtrack: DEBUG
    org.springframework.web: WARN

# ------------------------------------------------------------
# 개발자 정보. 개인정보라 저장소에 담지 않고 secrets.properties 에서 주입한다.
# 값이 없으면 /api/developer 는 404 를 돌려준다.
# ------------------------------------------------------------
app:
  developer:
    name: ${DEV_NAME:}
    email: ${DEV_EMAIL:}
    role: ${DEV_ROLE:}
    organization: ${DEV_ORG:}
    github: ${DEV_GITHUB:}
